-- Generated by CSharp.lua Compiler
local System = System
local ET = ET
local UnityEngine = UnityEngine
local ListComponent_1Vector3 = ET.ListComponent_1(UnityEngine.Vector3)
local ET
System.import(function (out)
  ET = out.ET
end)
System.namespace("ET", function (namespace)
  namespace.class("M2C_PathfindingResultHandler", function (namespace)
    local Run
    Run = function (this, session, message)
      return System.async(function (async, this, session, message)
        local unit = ET.UnitComponentSystem.Get(session:getDomain():GetComponent(ET.UnitComponent), message.Id)

        local speed = unit:GetComponent(ET.NumericComponent):GetAsFloat(ET.NumericType.Speed)

        System.using(ListComponent_1Vector3.Create(), function (list)
          for i = 0, #message.Xs - 1 do
            list.List:Add(UnityEngine.Vector3(message.Xs:get(i), message.Ys:get(i), message.Zs:get(i)))
          end

          async:Await(ET.MoveComponentSystem.MoveToAsync(unit:GetComponent(ET.MoveComponent), list.List, speed, 100))
        end)
      end, nil, this, session, message)
    end
    return {
      base = function (out)
        return {
          out.ET.AMHandler_1(out.ET.M2C_PathfindingResult)
        }
      end,
      Run = Run,
      __metadata__ = function (out)
        return {
          methods = {
            { "Run", 0x283, Run, out.ET.Session, out.ET.M2C_PathfindingResult, out.ET.ETVoid }
          },
          class = { 0x6, out.ET.MessageHandlerAttribute() }
        }
      end
    }
  end)
end)
