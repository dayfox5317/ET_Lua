-- Generated by CSharp.lua Compiler
local System = System
local Linq = System.Linq.Enumerable
System.namespace("ET", function (namespace)
  namespace.class("UnitComponentAwakeSystem", function (namespace)
    local Awake
    Awake = function (this, self)
    end
    return {
      base = function (out)
        return {
          out.ET.AwakeSystem_1(out.ET.UnitComponent)
        }
      end,
      Awake = Awake,
      __metadata__ = function (out)
        return {
          methods = {
            { "Awake", 0x106, Awake, out.ET.UnitComponent }
          },
          class = { 0x6 }
        }
      end
    }
  end)

  namespace.class("UnitComponentDestroySystem", function (namespace)
    local Destroy
    Destroy = function (this, self)
      for _, unit in System.each(self.idUnits:getValues()) do
        unit:Dispose()
      end

      self.idUnits:Clear()
    end
    return {
      base = function (out)
        return {
          out.ET.DestroySystem_1(out.ET.UnitComponent)
        }
      end,
      Destroy = Destroy,
      __metadata__ = function (out)
        return {
          methods = {
            { "Destroy", 0x106, Destroy, out.ET.UnitComponent }
          },
          class = { 0x6 }
        }
      end
    }
  end)

  namespace.class("UnitComponentSystem", function (namespace)
    local Add, Get, Remove, RemoveNoDispose, GetAll
    Add = function (self, unit)
      self.idUnits:AddKeyValue(unit.Id, unit)
      unit:setParent(self)
    end
    Get = function (self, id)
      local unit
      local _
      _, unit = self.idUnits:TryGetValue(id)
      return unit
    end
    Remove = function (self, id)
      local unit
      local _
      _, unit = self.idUnits:TryGetValue(id)
      self.idUnits:RemoveKey(id)
      local default = unit
      if default ~= nil then
        default:Dispose()
      end
    end
    RemoveNoDispose = function (self, id)
      self.idUnits:RemoveKey(id)
    end
    GetAll = function (self)
      return Linq.ToArray(self.idUnits:getValues())
    end
    return {
      Add = Add,
      Get = Get,
      Remove = Remove,
      RemoveNoDispose = RemoveNoDispose,
      GetAll = GetAll,
      __metadata__ = function (out)
        return {
          methods = {
            { "Add", 0x20E, Add, out.ET.UnitComponent, out.ET.Unit },
            { "Get", 0x28E, Get, out.ET.UnitComponent, System.Int64, out.ET.Unit },
            { "GetAll", 0x18E, GetAll, out.ET.UnitComponent, System.Array(out.ET.Unit) },
            { "Remove", 0x20E, Remove, out.ET.UnitComponent, System.Int64 },
            { "RemoveNoDispose", 0x20E, RemoveNoDispose, out.ET.UnitComponent, System.Int64 }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
